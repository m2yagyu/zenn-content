# 執筆プロジェクトの方針

## このリポジトリの目的
AIの原理を「やさしいのに正確」に伝えるコンテンツを継続的に出す。
統計力学・微分方程式などの物理・数学的バックグラウンドは、
"やさしいのに正確"を実現するための差別化の武器として活かす
（拡散モデルなど物理由来のトピックはその代表例であり、目的そのものではない）。

## 宛先の切り分け（2026-08-04 改定。ここを間違えると届かない）
媒体ごとに読者が違う。**1つの題材を調査・コード検証まで1回だけ行い、
出力を2つの宛先に書き分ける**。題材を2系統に分けて工数を倍にしない。

| 媒体 | 読者 | 出すもの |
|---|---|---|
| Zenn | **Pythonは書けるが、AIの中身は知らないエンジニア** | 実装・コード・数式まで踏み込む |
| Instagram | プログラミング未経験の完全初心者 | 直感とアナロジーのみ。数式もコードも出さない |

改定の根拠: Zennの`初心者`トピックを実測したところ、「未経験・非エンジニア」を
掲げて伸びている記事は全て**エンジニアが未経験を題材に書いた記事**であり、
未経験者に向けた入門記事ではなかった。Zennにおける「初心者」とは
「プログラミング未経験者」ではなく「その技術の入門者であるエンジニア」を指す。
したがってZenn記事に「プログラミング未経験でもOK」と書くと、
実際の読者に「これはあなた向けではない」という信号を送ることになる。

## タイトルの付け方（2026-08-08 追加。実測にもとづく。`docs/zenn-analysis-2026-08.md`）

- **「：」で副題を足す構造を使わない**。これが最も効いている。
  個人アカウントでは「：」ありのWIN率17.3%に対し、なし55.0%。
  タイトル長を20-35字に揃えて比べても13.6% vs 70.1%で差は消えない。
  企業Publicationは「：」があっても勝つ（配信力が別にあるため）が、
  その書き方を個人が真似すると負ける。
  「：」が効くのはコロンの手前がすでに集客力を持つ場合だけ（例: 有名プロダクト名＋定量成果）。
  我々の題材ではその条件を満たさないので、**副題を足したくなったら本文の見出しに回す**
- **20〜30字に収める**。WIN中央値31字。50字を超えると不利側に偏る
- **口語・好奇心の語彙を1つ入れる**（「結局」「そもそも」「〜って何なの？」「〜って面白い」）。
  口語のみのタイトルはWIN率78.2%（母集団平均61.5%）。
  勝っている同ジャンル記事の実例:「VAEって結局何者なの？」「拡散モデルの理論って面白いよね」
  「深層学習が多層の膨大なパラメータを学習できる雰囲気を理解する」
- **論文調の名詞止めにしない**（「〜の導出」「〜論文解説」「〜Explained」「〜について」）。
  伸びなかった記事に集中している型
- **「〜してみた」「〜やってみた」を使わない**（WIN 1.9% / BASE 5.9%）
- 具体的なモデル名・製品名は入れてよい（WIN 54.6% / BASE 16.9%）。
  ただし型番の羅列（`FLUX.1-schnell`＋`Colab`＋`Hugging Face推論API`）で字数を使い切らない
- タイトルは「何をやったか」ではなく「読者が抱く問い」を書く。
  仕様の説明ではなく、疑問文かそれに近い形にする

## 記事の執筆スタイル（毎回守る）
- 冒頭2行で「この記事を読むと何ができるようになるか」を明示する
- **見出しを「セル1」「セル2」のような手順ラベルにしない**（2026-08-08 追加）。
  Zennはサイドバーに目次を出すので、目次が手順番号だけだと読む理由が提示されないまま終わる。
  勝っている記事で手順ラベル見出しを使うのは2.1%だけ。
  代わりに**疑問形の見出し**にする（同ジャンルのWIN 25.5% / BASE 12.4%）。
  例:「セル3：軽量U-Net」→「なぜ全結合ではダメで、U-Netが要るのか」
- **図をコードより多くする**（2026-08-08 追加）。
  同ジャンルの上位記事は図を10〜20枚使い、コードは0個のこともある
  （「拡散モデルの理論って面白いよね」108いいね: 図19枚・コード0個・数式678）。
  我々の既存記事は図1〜3枚に対しコード6〜21個で、比率が逆になっている
- **数式は減らさなくてよい**。上位記事はkatex要素を数百個使っている。
  不利なのは数式の量ではなく、数式が読者の問いに紐づいていないこと
- 動くコードを先に見せ、理論・数式は後半に置く
- 数式を出す前には必ず日本語で直感的な説明を添える
- **本文は8,000字以上を目安にする**。3,000字未満は最も不利（WIN 8.6% / BASE 27.9%）、
  1万字前後が最も勝ち側に偏る。h3で階層を作る（WIN中央値5個 / BASE 2個）
- **外部リンクを増やす**（WIN中央値9個 / BASE 3個）。論文・公式ドキュメント・関連記事へ繋ぐ
- Zennの読者は「Pythonは書けるが、AIの中身・数学・物理には不安がある」エンジニアを想定する。
  Pythonの文法やColabの使い方そのものは説明しない（それは初心者トラック側の役割）
- 「プログラミング未経験でもOK」「誰でもできる」といった、
  非エンジニアを名指しする表現をZenn記事のタイトル・冒頭で使わない
- 物理の概念（統計力学、微分方程式など）を持ち出すときは、
  まず身近なアナロジーで説明してから数式に入り、
  難しい内容でも"やさしく・正確に"伝わることを最優先する。
  この「やさしさ」は宛先を初心者に下げる意味ではなく、
  中級読者にも効く説明の質のことを指す

## コードの扱い（信用に直結するので厳守）
- 記事に載せるコードは必ずこのセッション内で実行して動作確認する
- 動作確認が取れていないコードは記事に含めない
- 実装が数式・理論を正しく反映しているか、対応関係をコメントで示す
- **コードブロックには作図処理まで含める**（2026-08-08 追加）。
  読者がコピペすれば記事と同じ図が出る状態にする。図を別スクリプトで作ると
  記事の数値と図がずれる（実際に乱数列の違いでずれた）
- 各コードブロックの先頭で乱数seedを明示し、
  `python3 scripts/verify_article_code.py articles/<slug>.md` で
  「本文に載せた出力が本当にそのコードから出るか」を機械的に照合してから公開する
- **コードを載せたら、必ず「効いている行」の解説を添える**（2026-08-08 追加）。
  見出しを「セル1／セル2」から疑問形に変えたとき、見出しに載っていた解説ごと
  落としてしまったことがある。容器を変えるときは中身を移し替える
  - 各コードブロックの直後に、要の数行だけを ` ```python:抜粋 ` として再掲し、
    それが数式のどこに対応するかを書く。`:抜粋` を付けると検証スクリプトの
    実行対象から外れる（断片なので実行できない）
  - **Pythonの文法そのものは説明しない**（`as np` とは、`_` とは、など）。
    読者はPythonを書けるエンジニアなので、説明すべきは
    「実装が理論のどこに対応するか」と「ここを間違えると壊れる」の2点だけ

## 強調記法の注意
- `**「〜」**` のように `**` の直後が `「` などの約物だと、CommonMarkの規則で
  強調と解釈されず `**` がそのまま表示される。`「**〜**」` と括弧を外に出す
- 強調はプレーンな `**` の多用を避け、本当に効かせたい箇所に絞る。
  文全体を強調すると強調が機能しなくなる

## 図解
- 拡散過程の可視化、分布の変化、アーキテクチャ図は
  matplotlib / mermaid で生成する
- **日本語ラベルは `matplotlib-fontja` を使う**（2026-08-08 改定）。
  `plt.rcParams["font.family"] = "Hiragino Sans"` の直接指定はmacOSでしか動かず、
  読者がColabで実行すると豆腐になる。
- **依存の導入は、それを使う最初のコードブロックの中で完結させる**（2026-08-08 追加）。
  インストール手順を本文や別のブロックに書くと、読者は最初のコードセルだけを
  コピーして `ModuleNotFoundError` に当たる（実際に起きた）。
  `!pip` はマジックコマンドで `.py` でも検証スクリプトでも動かないので使わない
  ```python
  try:
      import matplotlib_fontja  # noqa: F401
  except ModuleNotFoundError:
      import subprocess, sys
      subprocess.check_call([sys.executable, "-m", "pip", "install", "-q", "matplotlib-fontja"])
      import matplotlib_fontja  # noqa: F401
  ```
- **ライブラリのバージョン差で壊れる書き方を避ける**。
  たとえば `from_pretrained(..., dtype=...)` は新しいtransformersの書き方なので、
  Colabのバージョン次第で動かない。`model.float()` のように
  素のtorchで書ける方法があればそちらを選ぶ

## 公開フロー
- Zennのフロントマター（title, emoji, type, topics, published）を整える
- topics 5枠は **流入3 + 転換2** で組む
  （`python3 scripts/zenn_analyze.py` の実測。詳細は `docs/zenn-analysis-2026-08.md`）
  - 流入枠: `生成ai`, `chatgpt` または `claude`, `llm`
  - 転換枠: `deeplearning`, `huggingface`, `物理`, `diffusion` から2つ
  - **`deeplearning` を最優先で入れる**。天井1,032（`machinelearning`と同じ）でありながら
    回転29日・転換率15.3%・いいね0率24.8%と、天井と通過率を両立している唯一のトピック
    （`machinelearning` は同じ天井でも回転6日・転換率2.6%・いいね0率55.6%）
  - `python`（いいね0率81%）と `ai` は汎用すぎるので使わない。
    特に `ai` は天井が最も高い（歴代1位2,325）が、新着48件が**0日**で入れ替わる。
    同じ日に48本投稿されるので、新着経由の露出は数時間で消える
  - `machinelearning` は天井1,032と高いが、30日後の10いいね到達率3.1%・いいね0率50.8%。
    母数は大きいが通過率が低いので、流入枠としては `llm`（天井1,072）を優先する
  - **転換枠の有効性は実測で裏付けが取れている**（2026-08-08）。
    30日後に10いいねへ到達する率は `huggingface` 19.0%・`物理` 13.1%・`diffusion` 11.9% で全トピック中最上位。
    新着に長く居座れること（物理240日、diffusion1,348日で48件分）が効いている
  - **ただし転換枠に流入を期待しない**。`物理` は歴代1位が38いいね、
    `diffusion` は歴代10位が3いいね。このトピックだけでは天井が数十で頭打ちになる
- 公開前に一度ローカルプレビュー（npx zenn preview）で確認する
