# 記事のコード検証用のプロジェクト専用環境（.venv）の依存を固定したもの。
#
#   python3 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt
#
# 直接インストールしたのは numpy / matplotlib / matplotlib-fontja / torch /
# transformers の5つだけで、
# 残りはその依存として入ったもの。
# .venv は .gitignore 済み。消しても このファイルから作り直せる。
#
# 2026-08-08 に .venv を作り直したため、記事ごとに必要なものだけを入れ直している。
# 過去記事(MNISTのU-Net等)を再検証するときは torchvision / scikit-learn を追加する。
annotated-doc==0.0.5
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
contourpy==1.3.3
cycler==0.12.1
filelock==3.32.2
fonttools==4.63.0
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.27.0
idna==3.18
Jinja2==3.1.6
kiwisolver==1.5.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
matplotlib==3.11.1
matplotlib-fontja==1.1.0
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.1
packaging==26.3
pillow==12.3.0
Pygments==2.20.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
regex==2026.7.19
rich==15.0.0
safetensors==0.8.0
setuptools==83.0.0
shellingham==1.5.4
six==1.17.0
sympy==1.14.0
tokenizers==0.22.2
torch==2.13.0
tqdm==4.70.0
transformers==5.14.1
typer==0.27.1
typing_extensions==4.16.0
